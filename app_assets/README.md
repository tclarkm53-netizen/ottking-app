# App Assets

Place your launcher icon here as `icon.png`.

GitHub Actions will automatically detect the file and regenerate Android launcher icons using `flutter_launcher_icons`.

If you want to change the icon, replace `app_assets/icon.png` and push the changes.
